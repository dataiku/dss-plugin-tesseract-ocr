{
    "meta": {
        "label": "Text extraction",
        "description": "Extract text from documents stored as PDF/Docx/HTML/etc into a dataset containing filenames and text.",
        "icon": "icon-file-text-alt",
        "displayOrderRank": 1
    },

    "kind": "PYTHON",
    "selectableFromFolder": "input_folder",

    "inputRoles" : [
        {
            "name": "input_folder",
            "label": "Input folder",
            "arity": "UNARY",
            "required": true,
            "description": "Folder of input files",
            "acceptsManagedFolder": true,
            "acceptsDataset": false
        }
    ],

    "outputRoles" : [
        {
            "name": "output_dataset",
            "label": "Output dataset",
            "arity": "UNARY",
            "required": true,
            "description": "Dataset of extracted text",
            "acceptsManagedFolder": false,
            "acceptsDataset": true
        }
    ],
    "params": [
        {
            "name": "extract_chunks",
            "label" : "Extract text chunks",
            "type": "BOOLEAN",
            "description": "Output one row per chunk alongside metadata."
        }
    ],

    "resourceKeys": []

}